MONGODB_URI=mongodb://localhost:27017/ai-summarize-rag
JWT_SECRET=your_super_secret_jwt_key
JWT_EXPIRE=30d
AI_SERVICE_URL=http://localhost:8000
PORT=5000
NODE_ENV=development

# ── DEMO MODE ──────────────────────────────────────────────────────────────
# Set to true to run without MongoDB (local file-backed demo auth).
# Set to false (or remove) to use the normal MongoDB connection above.
DEMO_MODE=false
